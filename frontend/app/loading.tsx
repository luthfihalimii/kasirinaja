export default function Loading() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-[var(--c-bg)]">
      <p className="text-sm text-[var(--c-text-muted)]">Memuat terminal...</p>
    </div>
  );
}
